import type { Meta, StoryObj } from '@storybook/react'

import NotHomePage from './NotHomePage'

const meta: Meta<typeof NotHomePage> = {
  component: NotHomePage,
}

export default meta

type Story = StoryObj<typeof NotHomePage>

export const Primary: Story = {}
